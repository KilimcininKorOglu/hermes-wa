name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Install zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.14.1

      - name: Lint
        run: make lint

      - name: Build Linux and Windows
        run: make build-linux build-windows

      - name: Verify binaries
        run: |
          echo "=== Linux amd64 ==="
          ls -la bin/charon_linux_amd64 bin/worker_linux_amd64
          echo "=== Linux arm64 ==="
          ls -la bin/charon_linux_arm64 bin/worker_linux_arm64
          echo "=== Windows amd64 ==="
          ls -la bin/charon_windows_amd64.exe bin/worker_windows_amd64.exe
